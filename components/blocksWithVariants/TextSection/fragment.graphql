fragment TextSection on TextSectionRecord {
  textSectionTitle: title
  textSectionText: text
  textVariant
  textBlockSize
  link {
    url
    label
  }
  bgColor {
    hex
  }
  textColor {
    hex
  }
}
